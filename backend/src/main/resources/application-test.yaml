spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tododb_test?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Cks9818765!
  jpa:
    hibernate:
      ddl-auto: create-drop
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2s

app:
  redis:
    ttl:
      todo-all-seconds: 60
      todo-by-id-seconds: 300
      session-seconds: 86400
jwt:
  secret: "testsecretkey-must-be-at-least-32-characters-long"
  expiration-ms: 86400000


