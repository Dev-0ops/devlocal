server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tododb?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Cks9818765!

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 2s

jwt:
  secret: "todosecretkey-must-be-at-least-32-characters-long"
  expiration-ms: 86400000

admin:
  username: "admin"
  password: "admin1234!"

app:
  redis:
    ttl:
      todo-all-seconds: 60
      todo-by-id-seconds: 300
      session-seconds: 86400

logging:
  level:
    com.todo: DEBUG
    org.hibernate.SQL: DEBUG
